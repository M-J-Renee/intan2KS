from pathlib import Path

DATA_ROOT = Path(#path to where .dat data is stored)
    
OUTPUT_ROOT = Path(#path to where you want kilosort output to be)
    
PROBE_PATH = Path(#path to channel map) 
    